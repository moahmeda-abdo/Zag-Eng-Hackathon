import Joi from 'joi';
import { UpdateCategoryData } from "../../../models/category/interfaces/category_document.interface";

export const UpdateCategoryValidationSchema = Joi.object<UpdateCategoryData>({
 
});
