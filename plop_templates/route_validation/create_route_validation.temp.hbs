import Joi from 'joi';
import { Create{{properCase name}}Data } from "../../../models/{{snakeCase name}}/interfaces/{{snakeCase name}}_document.interface";

export const Create{{properCase name}}ValidationSchema = Joi.object<Create{{properCase name}}Data>({
 
});